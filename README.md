# m26_prototype

This repository contains the Round 1 PoC pipeline. To demonstrate the intervention logic without physical onsite deployment, the `core_api` currently ingests data from a simulated BLE mesh node. The `edge_cv_processor` & `predictive_engine` ML models are architected for deployment onsite during Round 2.
